import React from 'react'

export const Komponendua = ({
  title
  // atau props
}) => {
  // passing props
  // bisa dengan props.title juga dengan syarat yang dipassing adalah props
  return (
    <div>
        {title}
    </div>
  )
}

